{% extends 'base.html.twig' %}

{% block main %}
    <p>Create new Article</p>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Save</button>
    {{ form_end(form) }}
{% endblock %}