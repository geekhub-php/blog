{% extends 'header.html.twig' %}

{% block body %}

    {% block leftside %}

    {% endblock leftside %}
        <div class="col-md-4">

            <!-- Blog Search Well -->
            <div class="well">
                <h4>Blog Search</h4>
                <div class="input-group">
                    <input type="text" class="form-control">
                                <span class="input-group-btn">
                                    <button class="btn btn-default" type="button">
                                        <span class="glyphicon glyphicon-search"></span>
                                </button>
                                </span>
                </div>
                <!-- /.input-group -->
            </div>
        {{ render(controller('AppBundle:Category:getAll')) }}
        </div>
    </div>
<footer>
    <div class="row">
        <div class="col-lg-12">
            <p class="text-center">Copyright &copy; Eugene Pavlyuk, {{ 'now'|date('Y') }}</p>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
</footer>
{% endblock body %}

