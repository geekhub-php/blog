{% extends 'admin/index_admin.html.twig' %}
{% block startMainDiv %}
    <div class="row"  id="idMainDiv">
        <div class="col-md-9 last-post-block" >
        <div class="contentOneMenu">
        <span class="label label-default spanMeny">forms show:</span>

            <div class="contentOneMenu">
                <h1>commeent edit </h1>

                            <table border="1px solid grey">
                            <thead>
                            <tr>
                                <th>ID</th>
                                <th>name</th>
                                <th>edit or delete</th>

                            </tr>
                            </thead>
                            <tbody>
                            {% for key in comments %}

                                <tr>
                             {% if userAcl.role.name =="ROLE_ADMIN" or userAcl.id==key.user.id %}

                                    <td>{{ key.id }}</td>
                                    <td>{{ key.text }}</td>
                                    <td> <a href="{{ path('comment_edit_admin', { 'id': key.id }) }}">Edit or Delete</a></td>
                                 {%endif %}


                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>


              {#  {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}

                <button type="submit">Edit</button>
                {{ form_end(edit_form) }} #}

            </div>

    </div>
    </div>
    </div>
{%endblock%}
{% block left %}
{% endblock%}
{% block right%}
{% endblock %}
